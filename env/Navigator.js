// Navigator
globalMy.Navigator_get_vendorSub = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['vendorSub'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_vendorSub,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_productSub = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['productSub'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_productSub,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_vendor = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['vendor'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_vendor,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_maxTouchPoints = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['maxTouchPoints'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_maxTouchPoints,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_scheduling = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['scheduling'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_scheduling,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_userActivation = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['userActivation'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_userActivation,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_doNotTrack = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['doNotTrack'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_doNotTrack,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_geolocation = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['geolocation'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_geolocation,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_connection = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['connection'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_connection,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_plugins = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['plugins'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_plugins,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_mimeTypes = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['mimeTypes'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_mimeTypes,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_pdfViewerEnabled = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['pdfViewerEnabled'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_pdfViewerEnabled,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_webkitTemporaryStorage = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['webkitTemporaryStorage'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_webkitTemporaryStorage,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_webkitPersistentStorage = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['webkitPersistentStorage'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_webkitPersistentStorage,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_hardwareConcurrency = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['hardwareConcurrency'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_hardwareConcurrency,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_cookieEnabled = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['cookieEnabled'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_cookieEnabled,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_appCodeName = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['appCodeName'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_appCodeName,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_appName = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['appName'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_appName,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_appVersion = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['appVersion'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_appVersion,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_platform = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['platform'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_platform,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_product = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['product'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_product,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_userAgent = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['userAgent'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_userAgent,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_language = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['language'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_language,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_languages = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['languages'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_languages,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_onLine = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['onLine'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_onLine,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_webdriver = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['webdriver'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_webdriver,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_getGamepads = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_getGamepads, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_javaEnabled = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_javaEnabled, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_sendBeacon = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_sendBeacon, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_vibrate = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_vibrate, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_bluetooth = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['bluetooth'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_bluetooth,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_clipboard = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['clipboard'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_clipboard,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_credentials = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['credentials'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_credentials,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_keyboard = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['keyboard'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_keyboard,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_managed = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['managed'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_managed,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_mediaDevices = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['mediaDevices'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_mediaDevices,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_storage = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['storage'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_storage,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_serviceWorker = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['serviceWorker'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_serviceWorker,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_wakeLock = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['wakeLock'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_wakeLock,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_deviceMemory = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['deviceMemory'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_deviceMemory,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_ink = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['ink'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_ink,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_hid = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['hid'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_hid,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_locks = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['locks'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_locks,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_mediaCapabilities = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['mediaCapabilities'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_mediaCapabilities,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_mediaSession = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['mediaSession'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_mediaSession,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_permissions = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['permissions'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_permissions,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_presentation = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['presentation'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_presentation,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_serial = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['serial'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_serial,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_virtualKeyboard = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['virtualKeyboard'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_virtualKeyboard,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_usb = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['usb'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_usb,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_xr = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['xr'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_xr,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_get_userAgentData = function () {
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['userAgentData'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_get_userAgentData,result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_canShare = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_canShare, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_share = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_share, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_clearAppBadge = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_clearAppBadge, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_setAppBadge = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_setAppBadge, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_getBattery = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_getBattery, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_getInstalledRelatedApps = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_getInstalledRelatedApps, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_getUserMedia = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_getUserMedia, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_requestMIDIAccess = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_requestMIDIAccess, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_requestMediaKeySystemAccess = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_requestMediaKeySystemAccess, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_webkitGetUserMedia = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_webkitGetUserMedia, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_registerProtocolHandler = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_registerProtocolHandler, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.Navigator_unregisterProtocolHandler = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了Navigator_unregisterProtocolHandler, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};

// History