globalMy.IDBFactory_cmp = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了IDBFactory_cmp, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.IDBFactory_databases = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了IDBFactory_databases, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.IDBFactory_deleteDatabase = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了IDBFactory_deleteDatabase, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};
globalMy.IDBFactory_open = function () {
  var result;
  //这里写方法实体

  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了IDBFactory_open, arguments => ', arguments, '  result => ', '' + result);
  }
  return result;
};